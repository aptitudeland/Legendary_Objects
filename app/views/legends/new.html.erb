<h1 class= "modification-titre-add"> Mettez votre objet sur Legendary Object</h1>

<div id="object_card">
<%= simple_form_for(@legend) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <% categories = ['Armes', 'Vêtements-Chaussures', 'Bijoux & accessoires ', 'Moyen de transport'] %>
  <%= f.input :category, as: :select, collection: categories %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit %>

<% end %>
</div>
